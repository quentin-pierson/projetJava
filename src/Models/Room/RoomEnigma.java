package Models.Room;

public class RoomEnigma extends Room{
    public RoomEnigma(String name, String description,int order, boolean isLocked, RoomExisting roomExisting) {
        super(name, description, order, isLocked, roomExisting);
    }
}
